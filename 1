function samArray(){
  let arr=[];
while(true){
  let number=+prompt("Enter number");
  if(number===""||number===null||isFinit(number)){
    break;
  }
  arr.push(number);
} 
  return arr;
}
console.log(sumArray());
